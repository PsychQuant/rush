{
  "rush": {
    "type": "stdio",
    "command": "${CLAUDE_PLUGIN_ROOT}/bin/rush-wrapper.sh",
    "description": "Rush — Taiwan TDX transport routing MCP (rail / bus / bike / air / traffic / parking)"
  }
}
